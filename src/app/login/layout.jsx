

export const metadata = {
    title: "Login",
    description: ""
}
  
  export default function LoginLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    );
  } 